<!-- fw-report
round: 030-edison-two-historical-scripts
role: verifier
branch: verifier/030-edison-two-historical-scripts
head: 6a5d57673f98ce1f6512d6b463e89f4c231157ae
os: macOS 27.0
python: 3.13.15
written: 2026-09-24T06:56:03Z
-->
Reviewed commit: `6a5d57673f98ce1f6512d6b463e89f4c231157ae` (`origin/builder/030-edison-two-historical-scripts`, as pinned by `fw.py start`), reviewed against `origin/main` `12e4f65` as a whole, since round 029 was never accepted. Code head: `de08abf`; the two later commits are the Builder's report only.

Commands I ran myself, from my own checkout (`.worktrees/verifier`, branch `verifier/030-edison-two-historical-scripts`). `python3` here is 3.9.6, so every project command used `/opt/homebrew/bin/python3.13` (3.13.15):

- `python3 tools/fw.py start --role verifier --round 030-edison-two-historical-scripts` — exit 0.
- `git diff --quiet origin/main HEAD -- data/errata/night-assailant.json` — exit 0 (byte-identical). `git diff … -- dist/lflists/2005-04-goat.lflist.conf dist/lflists/2011-09-tengu.lflist.conf` — exit 0 (identical). `git diff origin/main HEAD -- dist/lflists/` shows only the four Edison lines `-3897065`, `-50705071`, `+600000001`, `+600000002`; `16226786 1 --Night Assailant` is still at line 66.
- `git grep -n 600000003 -- data dist retroformats tests scripts schemas .github formats` — exit 1, no matches. In `docs/` the only matches are this round's brief and round 029's reports. `ls` of `data/custom-cards`, `dist/scripts`, `dist/databases`, and `sqlite3` on the `.cdb`: two files per card, two rows (`600000001`, `600000002`) in both tables. `data/cards/index.json` diff vs `main` adds only those two rows.
- `python -m unittest discover -t . -s tests` — exit 0, `Ran 1052 tests … OK (skipped=40)` (skips are the 40 engine tests).
- `python -m retroformats validate` — exit 0, `0 errors, 569 warnings`; `main` in a scratch worktree gives the same. Sorted warning diff vs `main`: exactly two Edison `format.erratum-known-divergence` lines gone (Metalzoa, Super Vehicroid - Stealth Union) and two GOAT `format.parity-omits-historical` lines added (the same two cards). Nothing else.
- `python -m retroformats build --check` — exit 0. `python3 tools/fw.py check` — `0 error(s), 0 warning(s)`.
- `python scripts/engine_env.py prepare --dest <scratch>/engine` — exit 0 (ocgcore built from the pinned source, macOS arm64); `run … --expect-at-least 40` — exit 0, `engine-tests: executed=40 skipped=0 failures=0 errors=0`.
- **Engine mutation runs (my own; each reverted with `git checkout -- dist`, tree clean):** replaced `dist/scripts/c600000001.lua` with Ignis's real modern `official/c50705071.lua` → `test_historical_metalzoa_cannot_be_revived_by_monster_reborn` fails (1 failure). Replaced `c600000002.lua` with `official/c3897065.lua` → `test_historical_card_cannot_use_its_equip_effect_when_only_the_opponent_has_a_monster` and `test_historical_card_offers_only_its_own_monster_as_an_equip_target` fail (2 failures). Same failures as round 029's red runs `35898264802` and `35898274208`, so the tests still fail on the modern behaviour. (I am the Verifier, so reading Ignis's scripts is my job; the brief only forbids it for the Builder.)
- **Materializer mutation runs**, on `python -m unittest tests.test_migration_materializer` (baseline `Ran 17 … OK`). Each stray edit, applied to the on-disk record and reverted afterwards, made `test_every_target_matches_the_real_on_disk_file_exactly` fail with `Lists differ: [] != ['<record id>']`: metalzoa `coverage.script`; metalzoa `implementation_metadata[0].reason` + `" x"`; metalzoa `…status` → `complete`; stealth-union an extra key in `coverage`; stealth-union `…tested` → `false`; stealth-union `review.notes` + `"x"` (a field outside the two); and a `reference_identities` entry re-added to `night-assailant.json`. Control: re-serialising all three records with no change leaves `git status` empty and the test green, so the failures come from the edits, not from formatting.
- `python3 sim.py` (my script): normalised each remaining script and its Ignis counterpart from CardScripts revision `383bfbd6…` (whitespace collapsed, comments and blank lines dropped) and measured line ratio, identifier-blind ratio (function/local names replaced by placeholders), identical lines, longest identical run and shared 4-line windows. Results below, with the removed Night Assailant script (`git show 8f1deb6:data/custom-cards/c600000003.lua`) as calibration.
- `gh api …/commits/6a5d576…/check-runs` — `check (3.10)`, `check (3.13)`, `engine` all success at the reviewed head. `gh run list` shows run `35966114727` (code head `de08abf`) also green.
- Read `git diff origin/main HEAD` for the materializer, shadow, Tengu, Yugi-Kaiba and custom-card tests, `docs/errata.md`, `docs/roadmap.md`, `docs/engine-testing.md`, `dist/README.md`, `data/cards/index.json`, `.github/workflows/ci.yml` and `docs/state.md`; and `git diff 8f1deb6 HEAD` for the engine test module. `git diff --quiet 8f1deb6 HEAD` on `c600000001/2.{lua,json}` and their `dist/scripts` copies: both scripts and records are unchanged since round 029 and each dist copy is byte-identical to its canonical script.

| script vs Ignis | line ratio | identifier-blind | identical lines | longest identical run | shared 4-line windows |
|---|---|---|---|---|---|
| Metalzoa (`c600000001`) | 0.27 | 0.41 | 13 of 47 | 3 | 0 of 44 |
| Stealth Union (`c600000002`) | 0.31 | 0.44 | 35 of 87 | 4 | 1 of 84 |
| Night Assailant (removed) | 0.69 | 0.73 | 38 of 49 | 7 | 11 of 46 |

## Findings

- [NOTE] Night Assailant is fully gone. `data/errata/night-assailant.json` equals `main`; no generated card, script, row, index entry, list line or test mentions it as implemented; `dist/README.md`, `docs/errata.md`, `docs/engine-testing.md` and `docs/roadmap.md` item 7 now say two cards and that it is held back (roadmap points at round 029's Verifier report and says why). The GOAT-parity hazard is kept without naming a card. The Edison list names `16226786` with count 1, and GOAT and Tengu are byte-identical to `main`.
- [NOTE] The materializer test now pins exact content. For the two records, `expected_after_round_029()` derives the expected record from the frozen target with only `coverage` and the single `implementation_metadata` entry replaced, and the `reason` is rebuilt from the frozen `gap_reason`/`gap_sources`. Every other record, including Night Assailant, is compared to its target as on `main`, and the byte comparison is kept. My seven mutations all fail it and the no-op control passes, so this closes my round 029 finding.
- [NOTE] The two remaining scripts still differ from Ignis's in structure, not only in names. The longest identical runs are 3 and 4 lines and are `end` plus a function signature and a `local c=e:GetHandler()` line; Metalzoa shares no 4-line window and Stealth Union one (a signature run), against 11 for the removed script. Mechanism differs too. Metalzoa: Ignis uses `CheckReleaseGroup` / `SelectReleaseGroup` with a separate `sptg` target and `KeepAlive`; this one uses `SelectMatchingCard` plus `Release` and no target function, and adds `EFFECT_CANNOT_SUMMON`, `EFFECT_CANNOT_MSET` and `EFFECT_SPSUMMON_CONDITION = aux.FALSE`, which Ignis's script does not have. Stealth Union: Ignis uses `aux.AddEREquipLimit`, `EquipByEffectAndLimitRegister` and an `EFFECT_TYPE_EQUIP` marker effect read back with `IsHasEffect(id)`; this one uses `Duel.Equip`, its own `EFFECT_EQUIP_LIMIT` and `RegisterFlagEffect`, and its equip filter is the behaviour the record needs (monsters you control only). What remains shared is what the card text dictates (the same four effects, the same fusion materials) and the ordinary target-effect scaffold used by nearly every EDOPro script. This narrows my round 029 SHOULD FIX on Stealth Union to nothing I would block on. I still cannot prove independent authorship; see Not verified.
- [NOTE] Engine floor 45 → 40 equals the five removed tests. `git diff 8f1deb6 HEAD` on the engine module removes exactly `NightAssailantEdisonTest` (five tests); `ci.yml`, `docs/engine-testing.md` and the module agree on 40. The Metalzoa and Stealth Union classes are untouched, so nothing was weakened.
- [NOTE] The net diff against `main` contains a `docs/state.md` change that is Brain's queue commit `6c4f5a2`, not the Builder's, and it is outside the brief's scope. It adds the licence decision (correct and useful) but also drops some durable text that is not in this round's brief: the sentence that UDE Appendix A is the August 1, 2005 revision (it survives in `docs/roadmap.md` item 1 and the archived round 13 brief), the naming of the three Limited-to-1 cards and the `battle_calculation` / `deck_out` axis statuses in the Tokyo Dome paragraph (they survive in `docs/research/yugi-kaiba-format-source-gate.md`), and the clause "overriding the framework's fresh-context preference" in the standing-chats decision (this one now survives nowhere). The file is exactly 1,000 words and `fw.py check` is clean. Brain should decide whether those removals were intended; none affects this round's product content.
- [NOTE] Agrees with the Builder: the warning swap, the 40-test count, the removed-test list, the `grep 600000003` result and its docs matches, and the stray-edit list for the materializer test (I reproduced each and added three more). The Builder's report says `docs/state.md` "was not changed"; that is true of its own commits and consistent with the above.

## Not verified

- Independent authorship of the two remaining scripts. I measured structure and mechanism against Ignis's scripts; I cannot know how the files were written, and the Builder was barred from opening Ignis's scripts this round, so the scripts are unchanged from round 029, when the Builder said it had not opened them either. Low similarity is evidence, not proof, and the licence reading stays the owner's.
- A real EDOPro client loading `dist/databases/retro-formats.cdb` and `dist/scripts/` through `data_path` / `script_path`; only the headless engine ran.
- Period behaviour beyond the record texts (what each record's `not_reproduced` lists), and the Yugipedia texts, which I checked in round 029 and did not re-fetch here (no record text changed).
- Windows, and Python 3.10 locally (I relied on the `check (3.10)` job at the exact head).
- I did not re-run the full engine tests under a client-like Linux environment; CI's `engine` job at `6a5d576` and my macOS run agree.

## Verdict

I believe round 030 does what its brief asks and I have no blockers. Night Assailant is completely out: its record is byte-identical to `main`, no generated card, script, row, index entry, list line or test remains, and the docs say what is now true. The Edison list changes only for the two remaining cards, GOAT and Tengu are untouched, `validate` shows 0 errors and 569 warnings with exactly the expected two-for-two swap, `build --check` and the suite are green, and CI is green at the exact head. The tightened materializer test is genuinely exact: seven deliberate stray edits fail it and a no-op control does not. Both remaining scripts still fail their engine tests when replaced by Ignis's modern scripts, and against Ignis's scripts they share almost no consecutive lines and use different mechanisms (release and equip handling), unlike the removed Night Assailant script; that is why I do not repeat my earlier concern about Stealth Union. What I cannot settle is the legal question itself, which was always the owner's. The one loose end is Brain's `docs/state.md` edit, which dropped a few durable sentences outside this brief. I am confident in the measured results and moderately confident that the two scripts are independent work.
