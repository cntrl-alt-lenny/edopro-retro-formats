# Generated and canonical data are byte-exact LF; keep checkouts identical on
# every platform so `build --check` and json round-trips stay deterministic.
* text=auto eol=lf
*.png binary
*.jpg binary
*.cdb binary
